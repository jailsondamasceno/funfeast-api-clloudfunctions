{
  "database": {
    "rules": "regras"
  },
  "firestore": {
    "rules": "regras_banco",
    "indexes": "index_firestore"
  },
  "functions": {
    "predeploy": [
      "npm --prefix \"$RESOURCE_DIR\" run lint"
    ]
  },
  "storage": {
    "rules": " regras_storage"
  }
}
